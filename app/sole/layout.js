import Footer from "../../components/Footer";
import Navbar from "../../components/Navbar";

export default function SoleLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}