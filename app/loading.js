export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return (
        <>
            <div className="w-[100%] h-screen fixed top-0 left-0 bg-white grid place-items-center">
               <h1 className="text-6xl uppercase font-semibold">Loading</h1>
            </div>
        </>
    )
}